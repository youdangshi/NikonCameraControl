# 妮妮 v1.4.1

本版本针对 Nikon Z30 的 STA 实时取景做协议前置修正。

## 更新内容

- STA 模式启动实时取景前，强制设置相机应用模式、记录媒体和实时取景选择器。
- 保留 `0x9435 ChangeApplicationMode(1)` 和退出取景后的恢复流程。
- 新增完整的 PC 控制前置诊断，便于区分模式设置失败和实时取景命令本身导致的断网。

## 安装

下载 `Nini-1.4.1-debug.apk` 并安装到 Android 手机。该包为真机测试版。
