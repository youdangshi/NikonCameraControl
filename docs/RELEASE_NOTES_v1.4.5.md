# 妮妮 v1.4.5

本版本修复 Nikon 快门值单位错误。

## 修复内容

- Nikon PTP `ExposureTime (0x500D)` 的单位修正为 `100 微秒`，与相机显示一致。
- 修复相机显示 `1/40`、应用却显示 `1/4000` 的问题。
- 快门读取和写入都使用同一套 Nikon 单位换算，避免界面值正确但写回相机错误。
- 增加 `1/40` 快门编码和解码回归测试。

## 安装

下载 `Nini-1.4.5-debug.apk` 并安装到 Android 手机。该包为真机测试版。
